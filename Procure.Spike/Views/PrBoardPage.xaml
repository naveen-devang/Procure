<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Procure.Spike.Views.PrBoardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:Procure.Models"
    xmlns:local="using:Procure.Spike"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,0" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Purchase Requisitions" FontSize="26" FontWeight="SemiBold" />
                <TextBlock x:Name="CountText" Opacity="0.7" FontSize="12.5" />
            </StackPanel>
            <TextBox Grid.Column="1" x:Name="SearchBox" Width="280" PlaceholderText="Search PR no / description…"
                     VerticalAlignment="Center" TextChanged="SearchBox_TextChanged" />
        </Grid>

        <!-- Perf readout -->
        <Border Grid.Row="1" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                CornerRadius="6" Padding="12,8">
            <Grid ColumnSpacing="18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="FpsText" Text="frame: —" FontFamily="Consolas" />
                <TextBlock Grid.Column="1" x:Name="WorstText" Text="worst/1s: —" FontFamily="Consolas" />
                <TextBlock Grid.Column="2" x:Name="LoadText" Text="load: —" FontFamily="Consolas" />
                <Button Grid.Column="4" x:Name="ScrollStressButton" Content="Scroll stress test"
                        Click="ScrollStressButton_Click" />
            </Grid>
        </Border>

        <!-- Board -->
        <ListView
            Grid.Row="2"
            x:Name="Board"
            SelectionMode="None"
            IncrementalLoadingTrigger="Edge"
            IncrementalLoadingThreshold="3"
            DataFetchSize="3"
            ShowsScrollingPlaceholders="False"
            ContainerContentChanging="Board_ContainerContentChanging">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,10" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:PurchaseRequisition">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1" CornerRadius="8" Padding="16,12">
                        <Grid ColumnSpacing="14" RowSpacing="6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Text="{x:Bind PrNo}" FontWeight="SemiBold" FontSize="14"
                                       VerticalAlignment="Center" />

                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind PrimaryItemDisplay}" TextTrimming="CharacterEllipsis"
                                           MaxLines="1" FontSize="13.5" />
                                <TextBlock Text="{x:Bind Requestor}" Opacity="0.6" FontSize="11.5" />
                            </StackPanel>

                            <Border Grid.Column="2" Background="{ThemeResource AccentControlElevationBorderBrush}"
                                    CornerRadius="4" Padding="8,2" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Status}" FontSize="11" />
                            </Border>

                            <Border Grid.Column="3" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1" CornerRadius="4" Padding="8,2" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind Priority}" FontSize="11" />
                            </Border>

                            <TextBlock Grid.Column="4" Text="{x:Bind AgeDisplay}" Opacity="0.6"
                                       FontSize="11.5" VerticalAlignment="Center" />

                            <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4" Orientation="Horizontal"
                                        Spacing="16">
                                <TextBlock Text="{x:Bind FormattedTotalPoValue}" FontSize="12" FontWeight="SemiBold" />
                                <TextBlock FontSize="11.5" Opacity="0.6"
                                           Text="{x:Bind local:Fmt.Counts(RfqCount, PoCount, ItemsCount)}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
