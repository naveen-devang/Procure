<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:Procure.PageModels"
             x:Class="Procure.Pages.Modals.BatchPoModal"
             x:DataType="pageModels:PrListPageModel"
             IsVisible="{Binding IsBatchPoModalVisible}">

    <Grid BackgroundColor="#99000000">
        <Border MaximumWidthRequest="480"
                Margin="24"
                VerticalOptions="Center"
                HorizontalOptions="Center"
                StrokeThickness="1"
                Stroke="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray600}}"
                Background="{AppThemeBinding Light={StaticResource LightSecondaryBackground}, Dark={StaticResource DarkSecondaryBackground}}"
                Padding="26">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10" />
            </Border.StrokeShape>
            <Border.Shadow>
                <Shadow Brush="#30000000" Offset="0,8" Radius="20" Opacity="0.8" />
            </Border.Shadow>

            <VerticalStackLayout Spacing="14">
                <Label Text="Create Master Combined Purchase Order"
                       FontFamily="SegoeSemibold" FontSize="16"
                       TextColor="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}" />

                <VerticalStackLayout Spacing="4">
                    <Label Text="PO Number *" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}" />
                    <Entry Text="{Binding BatchPoNo, Mode=TwoWay}" Placeholder="e.g. PO-2026-901" HeightRequest="36" />
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="4">
                    <Label Text="Vendor Name *" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}" />
                    <Entry Text="{Binding BatchPoVendor, Mode=TwoWay}" Placeholder="Vendor Name" HeightRequest="36" />
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="4">
                    <Label Text="Total Combined PO Value ($) *" FontSize="12" TextColor="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}" />
                    <Entry Text="{Binding BatchPoTotalValue, Mode=TwoWay}" Placeholder="0.00" Keyboard="Numeric" HeightRequest="36" />
                </VerticalStackLayout>

                <HorizontalStackLayout Spacing="10" HorizontalOptions="End" Margin="0,10,0,0">
                    <Button Text="Cancel" Command="{Binding CloseBatchPoModalCommand}" Style="{StaticResource CancelButton}" HeightRequest="36" Padding="14,0" />
                    <Button Text="Create Combined PO" Command="{Binding SaveBatchPoModalCommand}" Style="{StaticResource PrimaryButton}" HeightRequest="36" Padding="18,0" />
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentView>
